// src/ts/GameState.ts

export default class GameState {
  static from(_object: object) {
    // TODO: create new instance from passed object
    // создать новый экземпляр из переданного объекта
    return new GameState();
  }
}
